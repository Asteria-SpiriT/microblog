<% include header.ejs %>
  <% include alert.ejs %>
    <% if (!locals.user) { %>
      <div class="jumbotron">
        <div class="container">
          <h1>欢迎来到 MicroBlog</h1>
          <p>这是一个基于Node.js 的微博系统.</p>
          <p>
            <a class="btn btn-primary btn-large" href="/login">登录</a>
            <a class="btn btn-default btn-large" href="/reg">立即注册</a>
          </p>
        </div>
      </div>
    <% } else { %>
      <% include say.ejs %>
    <% } %>

    <% include posts.ejs %>
<% include footer.ejs %>